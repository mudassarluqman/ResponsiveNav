// Selecting ham menu

let menu = document.querySelector(".ham-menu");

menu.addEventListener("click", (e) => {
  // selecting the nav menu and adding respo call to it
  let navMenu = document.querySelector(".nav-menu");
  navMenu.classList.toggle("respo");

  //   animation the ham menu icon
});
